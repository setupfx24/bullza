/** Dashboard / in-app product name. */
export const BRAND_NAME = 'SwisDex';

/** Zustand persist key for UI preferences (theme, terminal layout). */
export const STORAGE_KEY_UI = 'swisdex-ui';

/** Legacy key — migrated once on load; logout clears legacy auth key. */
export const STORAGE_KEY_UI_LEGACY = 'swisdex-ui';
